class Usuario {
  constructor(id, titulo, autor, ano_publicacao, quantidade_disponivel,) {
    this.id = id;
    this.titulo = titulo;
    this.autor = autor;
    this.ano_publicacao = ano_publicacao;
    this.quantidade_disponivel = quantidade_disponivel; // corrigido: antes estava "perfilt"
  }
}